/**
 * @tanstack/realtime-core
 *
 * Framework-agnostic primitives shared by the client and server packages.
 * This is the equivalent of @tanstack/query-core — the dependency-free
 * foundation that all adapters build on.
 */

export type { QueryKey } from './types.js'
export { serializeKey } from './serializeKey.js'
